{{extend 'layout.html'}}
<div id="content">
    <div class="post greenbox">
           <div class="title">
                <h1>Insertar Participante</h1>
           </div>
           <div class="entry">
                    {{=form}}
           </div>
           <div class="btm" align="center">
              <div class="l">
                  <div class="r">
                       <small>*Por favor llenar todos los campos solicitados</br></small>
                                       <small>{{=A('Regresar', _href=URL("index"))}}</small>
                  </div>
              </div>
           </div>
     </div>
</div>
<div id="sidebar">
  <ul>
    <li>
      <h2>
       Participantes Agregados
      </h2>
      <ul align="center">
        {{cont = 0}}
        {{for participante in participantes:}}
        {{cont += 1}} 
        {{if (cont < 5):}}
        {{=LI(A(participante.nombre, _href=URL("showParticipante", args=participante.id)))}}
        {{pass}}
        {{else:}}
        <small> {{=LI(A("Ver Mas", _href=(URL("mostrarParticipante"))))}}</small>
        {{pass}}        
      </ul>
    </li>
</div>
{{pass}}
